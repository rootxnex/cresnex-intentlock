# ABI

`CresnexIntentLockAccount.json` and `CresnexIntentLockAccountV2.json` are copied from Foundry output after compilation.

V1 and v2 are incompatible contracts and EIP-712 schemas. Consumers must select the ABI using explicit deployment metadata, never by guessing.
