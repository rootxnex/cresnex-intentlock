# Public merchant-recipient address only. Do not store a private key, seed phrase, or keystore password here.
X402_DEMO_RECIPIENT=
# Set only after reviewing the controlled resource's first test amount (atomic USDC units; 6 decimals).
X402_DEMO_AMOUNT_ATOMIC=
X402_DEMO_PORT=4021
